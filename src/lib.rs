pub mod rich_text;
pub mod text;
pub mod text_button;
pub mod image;
pub mod lined_flow;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
